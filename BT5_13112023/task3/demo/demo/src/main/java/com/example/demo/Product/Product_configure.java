package com.example.demo.Product;

import java.util.List;

import org.springframework.boot.CommandLineRunner;
import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;

@Configuration
public class Product_configure {
    // @Bean
    // CommandLineRunner commandLineRunner( Product_repository repository)
    // {
    //     return args -> {
    //         product pro1=new product(0, "msi", "For gay only", "http://", 100000, 0);
	// 		product pro2=new product(0, "msai", "For gay only", "http://", 100000, 0);
    //         repository.saveAll(List.of(pro1,pro2));
    //     };
    // }
}
